<!DOCTYPE html>
<html>
<head>
  <title><%= email.subject %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f8;
      margin: 0;
      padding: 0;
    }

    header {
      background: #0b5ed7;
      color: white;
      padding: 1rem;
      text-align: center;
      font-size: 1.5rem;
      font-weight: bold;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    main {
      max-width: 800px;
      margin: 2rem auto;
      background: white;
      padding: 1.5rem;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    h1 {
      margin-top: 0;
    }

    .meta {
      color: #555;
      font-size: 0.9rem;
      margin-bottom: 1rem;
    }

    pre {
      background: #f0f0f0;
      padding: 1rem;
      border-radius: 6px;
      overflow-x: auto;
      white-space: pre-wrap;
      word-wrap: break-word;
    }

    a.back {
      display: inline-block;
      margin-top: 1rem;
      text-decoration: none;
      color: #0b5ed7;
      font-weight: bold;
    }

    a.back:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <header>📧 Email Viewer</header>
  <main>
    <h1><%= email.subject %></h1>
    <div class="meta">
      <div><b>From:</b> <%= email.from?.text %></div>
      <div><b>To:</b> <%= email.to?.text %></div>
      <div><b>Date:</b> <%= email.date %></div>
    </div>
    <hr>
    <pre><%= email.text %></pre>
    <a href="/" class="back">← Back to inbox</a>
  </main>
</body>
</html>
<!DOCTYPE html>



<!-- <html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Details - TempMail</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 20px 24px;
            margin-bottom: 24px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            width: 36px;
            height: 36px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            color: white;
        }

        .logo-text {
            font-size: 20px;
            font-weight: 700;
            color: #2d3748;
        }

        .back-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 10px 16px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .back-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }

        .email-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 32px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .email-subject {
            font-size: 28px;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 24px;
            line-height: 1.3;
        }

        .email-meta {
            background: #f7fafc;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 32px;
            border-left: 4px solid #667eea;
        }

        .meta-row {
            display: flex;
            margin-bottom: 12px;
            align-items: center;
        }

        .meta-row:last-child {
            margin-bottom: 0;
        }

        .meta-label {
            font-weight: 600;
            color: #4a5568;
            min-width: 80px;
            font-size: 14px;
        }

        .meta-value {
            color: #2d3748;
            font-size: 14px;
            font-family: 'Courier New', monospace;
            background: white;
            padding: 4px 8px;
            border-radius: 6px;
            border: 1px solid #e2e8f0;
        }

        .email-content {
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            padding: 24px;
            font-size: 16px;
            line-height: 1.6;
            color: #2d3748;
            white-space: pre-wrap;
            word-wrap: break-word;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        .email-actions {
            margin-top: 24px;
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .action-btn {
            background: white;
            border: 2px solid #e2e8f0;
            color: #4a5568;
            padding: 10px 16px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 14px;
        }

        .action-btn:hover {
            border-color: #667eea;
            color: #667eea;
            transform: translateY(-1px);
        }

        .action-btn.primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border-color: transparent;
        }

        .action-btn.primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
            color: white;
        }

        @media (max-width: 768px) {
            .container {
                padding: 16px;
            }
            
            .header {
                flex-direction: column;
                gap: 16px;
                text-align: center;
            }
            
            .email-container {
                padding: 24px 20px;
            }
            
            .email-subject {
                font-size: 24px;
            }
            
            .meta-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 4px;
            }
            
            .meta-label {
                min-width: auto;
            }
            
            .email-actions {
                flex-direction: column;
            }
            
            .action-btn {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">
                <div class="logo-icon">📧</div>
                <div class="logo-text">TempMail</div>
            </div>
            <a href="inbox.html" class="back-btn">
                ← Back to Inbox
            </a>
        </div>

        <div class="email-container">
            <h1 class="email-subject">Welcome to our service!</h1>
            
            <div class="email-meta">
                <div class="meta-row">
                    <div class="meta-label">From:</div>
                    <div class="meta-value">welcome@service.com</div>
                </div>
                <div class="meta-row">
                    <div class="meta-label">To:</div>
                    <div class="meta-value">aman.temp.2024@tempmail.org</div>
                </div>
                <div class="meta-row">
                    <div class="meta-label">Date:</div>
                    <div class="meta-value">December 24, 2024 at 2:30 PM</div>
                </div>
                <div class="meta-row">
                    <div class="meta-label">Subject:</div>
                    <div class="meta-value">Welcome to our service!</div>
                </div>
            </div>

            <div class="email-content">Hello there!

Thank you for signing up for our service! We're excited to have you on board.

Here's everything you need to know to get started:

1. Your account is now active and ready to use
2. You can access your dashboard at any time
3. Our support team is available 24/7 if you need help

Getting Started:
• Complete your profile setup
• Explore our features and tools
• Join our community forum

If you have any questions, don't hesitate to reach out to our support team at support@service.com.

Welcome aboard!

Best regards,
The Service Team</div>

            <div class="email-actions">
                <button class="action-btn primary" onclick="replyEmail()">
                    📝 Reply
                </button>
                <button class="action-btn" onclick="forwardEmail()">
                    📤 Forward
                </button>
                <button class="action-btn" onclick="deleteEmail()">
                    🗑️ Delete
                </button>
                <button class="action-btn" onclick="markSpam()">
                    🚫 Mark as Spam
                </button>
            </div>
        </div>
    </div>

    <script>
        function replyEmail() {
            alert('Reply functionality would be implemented here');
        }

        function forwardEmail() {
            alert('Forward functionality would be implemented here');
        }

        function deleteEmail() {
            if (confirm('Are you sure you want to delete this email?')) {
                alert('Email deleted successfully');
                window.location.href = 'inbox.html';
            }
        }

        function markSpam() {
            if (confirm('Mark this email as spam?')) {
                alert('Email marked as spam');
                window.location.href = 'inbox.html';
            }
        }
    </script>
</body>
</html> -->
